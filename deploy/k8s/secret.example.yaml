# Example only. In production these come from a secrets manager via CSI (§13), NOT git.
apiVersion: v1
kind: Secret
metadata:
  name: aegis-secrets
  namespace: aegis
type: Opaque
stringData:
  AEGIS_SIGNING_SECRET: "replace-with-strong-random-secret"   # ADR-014 approval HMAC
  AEGIS_INGEST_TOKEN: "replace-with-ingest-token"
